# public/ ディレクトリについて

Next.jsでは、この `public` フォルダに置いたファイルが、そのままドメイン直下の
静的ファイルとして公開される（画像・favicon・ロボッツファイルなど）。

例:
- `public/logo.png` を置くと `/logo.png` としてアクセスできる
- `public/favicon.ico` を置くと自動的にブラウザタブのアイコンに使われる

現時点ではまだ画像素材が無いため空の状態。
